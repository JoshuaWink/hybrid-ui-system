/**
 * StatusCard Component
 * 
 * Displays a single metric (count + label) with status badge
 * Used for dashboard KPIs: jobs running, deployments today, agents healthy
 */

export { StatusCard } from './StatusCard';
export type { StatusType, StatusCardProps } from './StatusCard';
